# Flow Builder

A flow chart editor built with Vue 3, Vue Flow and TanStack Query.

```bash
npm install
npm run dev
```

Status: in progress. Work is broken into tickets in [task-chunks.md](task-chunks.md), and the
requirements are in [plan.md](plan.md).
