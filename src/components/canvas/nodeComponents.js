import FlowNodeCard from './FlowNodeCard.vue'

/** One component for every shape: they differ only in registry data and outline. */
export const nodeComponents = Object.freeze({ shape: FlowNodeCard })
